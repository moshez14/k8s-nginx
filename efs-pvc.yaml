apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-efs-pvc
  namespace: maifocus-ns-prod
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: efs-sc
  resources:
    requests:
      storage: 5Gi

